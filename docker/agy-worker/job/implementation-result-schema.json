{
  "type": "object",
  "properties": {
    "summary": {"type": "string"},
    "changes": {
      "type": "array",
      "minItems": 1,
      "maxItems": 20,
      "items": {
        "type": "object",
        "properties": {
          "path": {"type": "string"},
          "operation": {"type": "string", "enum": ["upsert", "delete"]},
          "content": {"type": "string"},
          "rationale": {"type": "string"}
        },
        "required": ["path", "operation", "content", "rationale"],
        "additionalProperties": false
      }
    },
    "tests_recommended": {"type": "array", "items": {"type": "string"}},
    "risks": {"type": "array", "items": {"type": "string"}}
  },
  "required": ["summary", "changes", "tests_recommended", "risks"],
  "additionalProperties": false
}
